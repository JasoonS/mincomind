type Players {
  id: ID!
  points: BigInt!
  numberOfGames: Int!
  active: Boolean! # false if the player withdraw their portion of the pot
}

type Tournament {
  id: ID! # singleton - mincomind
  pot: BigInt!
  players: [Players!]!
}
